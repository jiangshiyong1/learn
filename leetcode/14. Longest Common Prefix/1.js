/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function (strs) {
    let target = []
    for (let i = 0; i < strs.length; i++) {
        for (let j = 0; j < strs[i]; j++) {
            if (i == 0) {
                target.push(strs[i][j])
            } else {
                
            }
        }
    }
};